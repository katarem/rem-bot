const homosexual = "jano";
const eatenCocks = Math.floor(Math.random() * 100);
let añadido;
if (eatenCocks <= 50) {
  añadido = ", fueron pocas para él.";
} else {
  añadido = ", hoy tenía hambre.";
}
console.log(
  homosexual + " hoy se ha zampado " + eatenCocks + " pollas" + añadido
);
